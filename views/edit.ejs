<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body>
        <div class="w-full h-full bg-zinc-900 pt-10 text-white">
            <div class="nav mb-10 pl-4">
                <a class="text-blue-500" href="/read">Read Users</a>
            </div>
            <form action="/update/<%= users._id %>" method="post">
                <h1 class="text-3xl tracking-tighter mb-3 pl-4">Update User</h1>
                <input
                    class="border-zinc-300 py-2 px-5 m-6 rounded-lg bg-transparent"
                    type="text" placeholder="name" name="name" value="<%= users.name %>">
                <input
                    class="border-zinc-300 py-2 px-5 m-6 rounded-lg bg-transparent"
                    type="text" placeholder="image url" name="image" value="<%= users.image %>">
                <input
                    class="border-zinc-300 py-2 px-5 m-6 rounded-lg bg-transparent"
                    type="text" placeholder="email" name="email" value="<%= users.email %>">
                    <input class="py-2 px-5 bg-yellow-600 rounded-md" type="submit" value="Update User">
            </form>
        </div>
    </body>
</html>